# 魔鲸GEO 数据库表结构设计

## 1. 数据库规范

### 1.1 命名规范

| 类型 | 规范 | 示例 |
|------|------|------|
| 数据库 | 小写下划线 | `hiaeo_prod` |
| 表 | 小写下划线，复数 | `users`, `diagnose_reports` |
| 字段 | 小写下划线 | `user_id`, `created_at` |
| 索引 | `idx_` + 表名 + 字段 | `idx_users_phone` |
| 外键 | `fk_` + 表名 | `fk_diagnose_user` |

### 1.2 通用字段

```sql
-- 所有业务表必须包含
id          BIGINT UNSIGNED  AUTO_INCREMENT  PRIMARY KEY COMMENT '主键ID'
created_at  DATETIME         DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间'
updated_at  DATETIME         DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间'
deleted_at  DATETIME         DEFAULT NULL COMMENT '删除时间'
is_deleted  TINYINT(1)      DEFAULT 0 COMMENT '软删除标记'
```

---

## 2. 核心表结构

### 2.1 用户表 (users)

```sql
CREATE TABLE `users` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '用户ID',
  `phone` VARCHAR(20) NOT NULL COMMENT '手机号',
  `email` VARCHAR(255) DEFAULT NULL COMMENT '邮箱',
  `password_hash` VARCHAR(255) NOT NULL COMMENT '密码哈希',
  `nickname` VARCHAR(50) DEFAULT NULL COMMENT '昵称',
  `avatar_url` VARCHAR(500) DEFAULT NULL COMMENT '头像URL',
  `company_name` VARCHAR(200) NOT NULL COMMENT '公司名称',
  `company_domain` VARCHAR(255) NOT NULL COMMENT '公司官网域名',
  `industry` VARCHAR(50) DEFAULT NULL COMMENT '行业',
  `employee_count` INT DEFAULT NULL COMMENT '员工数量',
  `role` ENUM('user', 'admin', 'super_admin') DEFAULT 'user' COMMENT '角色',
  `status` ENUM('active', 'inactive', 'banned') DEFAULT 'active' COMMENT '账号状态',
  `last_login_at` DATETIME DEFAULT NULL COMMENT '最后登录时间',
  `last_login_ip` VARCHAR(50) DEFAULT NULL COMMENT '最后登录IP',
  `source` VARCHAR(50) DEFAULT 'web' COMMENT '来源渠道',
  `utm_source` VARCHAR(100) DEFAULT NULL COMMENT '营销来源',
  `utm_medium` VARCHAR(100) DEFAULT NULL COMMENT '营销媒介',
  `utm_campaign` VARCHAR(100) DEFAULT NULL COMMENT '营销活动',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `deleted_at` DATETIME DEFAULT NULL,
  `is_deleted` TINYINT(1) DEFAULT 0,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_phone` (`phone`),
  UNIQUE KEY `uk_email` (`email`),
  KEY `idx_company_domain` (`company_domain`),
  KEY `idx_status` (`status`),
  KEY `idx_created_at` (`created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='用户表';
```

### 2.2 认证令牌表 (auth_tokens)

```sql
CREATE TABLE `auth_tokens` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '令牌ID',
  `user_id` BIGINT UNSIGNED NOT NULL COMMENT '用户ID',
  `token_type` ENUM('access', 'refresh') NOT NULL COMMENT '令牌类型',
  `token_hash` VARCHAR(255) NOT NULL COMMENT '令牌哈希',
  `device_info` JSON DEFAULT NULL COMMENT '设备信息',
  `ip_address` VARCHAR(50) DEFAULT NULL COMMENT 'IP地址',
  `user_agent` VARCHAR(500) DEFAULT NULL COMMENT '浏览器UA',
  `expires_at` DATETIME NOT NULL COMMENT '过期时间',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_token_hash` (`token_hash`),
  KEY `idx_expires_at` (`expires_at`),
  CONSTRAINT `fk_auth_user` FOREIGN KEY (`user_id`) REFERENCES `users` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='认证令牌表';
```

### 2.3 套餐表 (packages)

```sql
CREATE TABLE `packages` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '套餐ID',
  `code` VARCHAR(50) NOT NULL COMMENT '套餐代码',
  `name` VARCHAR(100) NOT NULL COMMENT '套餐名称',
  `name_en` VARCHAR(100) DEFAULT NULL COMMENT '英文名称',
  `description` TEXT COMMENT '套餐描述',
  `tier` ENUM('basic', 'professional', 'enterprise') NOT NULL COMMENT '等级',
  `price_monthly` DECIMAL(10,2) NOT NULL DEFAULT 0.00 COMMENT '月付价格',
  `price_yearly` DECIMAL(10,2) NOT NULL DEFAULT 0.00 COMMENT '年付价格',
  `price_yearly_discount` DECIMAL(5,2) DEFAULT 80.00 COMMENT '年付折扣比例',
  `features` JSON NOT NULL COMMENT '功能列表',
  `limits` JSON NOT NULL COMMENT '限制列表',
  `diagnose_engines` INT DEFAULT 1 COMMENT '诊断引擎数',
  `diagnose_questions` INT DEFAULT 10 COMMENT '诊断问题数',
  `strategy_types` JSON DEFAULT NULL COMMENT '策略类型',
  `content_limit_monthly` INT DEFAULT 10 COMMENT '月内容生成上限',
  `competitor_limit` INT DEFAULT 1 COMMENT '竞品监控数',
  `hub_views` JSON DEFAULT NULL COMMENT 'Hub视图权限',
  `api_access` TINYINT(1) DEFAULT 0 COMMENT 'API权限',
  `dedicated_support` TINYINT(1) DEFAULT 0 COMMENT '专属顾问',
  `data_isolation` TINYINT(1) DEFAULT 0 COMMENT '数据隔离',
  `sort_order` INT DEFAULT 0 COMMENT '排序',
  `is_trial` TINYINT(1) DEFAULT 0 COMMENT '是否试用套餐',
  `trial_days` INT DEFAULT 7 COMMENT '试用天数',
  `is_recommended` TINYINT(1) DEFAULT 0 COMMENT '是否推荐',
  `status` ENUM('active', 'inactive', 'deprecated') DEFAULT 'active' COMMENT '状态',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_code` (`code`),
  KEY `idx_tier` (`tier`),
  KEY `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='套餐表';
```

### 2.4 订阅表 (subscriptions)

```sql
CREATE TABLE `subscriptions` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '订阅ID',
  `user_id` BIGINT UNSIGNED NOT NULL COMMENT '用户ID',
  `package_id` BIGINT UNSIGNED NOT NULL COMMENT '套餐ID',
  `subscription_no` VARCHAR(50) NOT NULL COMMENT '订阅编号',
  `billing_cycle` ENUM('monthly', 'yearly') NOT NULL COMMENT '计费周期',
  `status` ENUM('active', 'cancelled', 'expired', 'pending', 'trial') NOT NULL COMMENT '状态',
  `started_at` DATETIME NOT NULL COMMENT '开始时间',
  `expires_at` DATETIME NOT NULL COMMENT '过期时间',
  `cancelled_at` DATETIME DEFAULT NULL COMMENT '取消时间',
  `auto_renew` TINYINT(1) DEFAULT 1 COMMENT '自动续费',
  `is_trial` TINYINT(1) DEFAULT 0 COMMENT '是否试用',
  `trial_ends_at` DATETIME DEFAULT NULL COMMENT '试用结束时间',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_subscription_no` (`subscription_no`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_status` (`status`),
  KEY `idx_expires_at` (`expires_at`),
  CONSTRAINT `fk_subscription_user` FOREIGN KEY (`user_id`) REFERENCES `users` (`id`) ON DELETE CASCADE,
  CONSTRAINT `fk_subscription_package` FOREIGN KEY (`package_id`) REFERENCES `packages` (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='订阅表';
```

### 2.5 订单表 (orders)

```sql
CREATE TABLE `orders` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '订单ID',
  `order_no` VARCHAR(50) NOT NULL COMMENT '订单编号',
  `user_id` BIGINT UNSIGNED NOT NULL COMMENT '用户ID',
  `subscription_id` BIGINT UNSIGNED DEFAULT NULL COMMENT '订阅ID',
  `package_id` BIGINT UNSIGNED NOT NULL COMMENT '套餐ID',
  `product_name` VARCHAR(200) NOT NULL COMMENT '商品名称',
  `billing_cycle` ENUM('monthly', 'yearly') NOT NULL COMMENT '计费周期',
  `original_amount` DECIMAL(10,2) NOT NULL COMMENT '原价',
  `discount_amount` DECIMAL(10,2) DEFAULT 0.00 COMMENT '折扣金额',
  `coupon_id` BIGINT UNSIGNED DEFAULT NULL COMMENT '优惠券ID',
  `coupon_code` VARCHAR(50) DEFAULT NULL COMMENT '优惠券代码',
  `final_amount` DECIMAL(10,2) NOT NULL COMMENT '实付金额',
  `payment_method` ENUM('alipay', 'wechat', 'card', 'transfer') DEFAULT NULL COMMENT '支付方式',
  `payment_status` ENUM('pending', 'paid', 'refunded', 'failed') DEFAULT 'pending' COMMENT '支付状态',
  `payment_time` DATETIME DEFAULT NULL COMMENT '支付时间',
  `payment_no` VARCHAR(100) DEFAULT NULL COMMENT '支付流水号',
  `invoice_status` ENUM('none', 'requested', 'issued') DEFAULT 'none' COMMENT '发票状态',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_order_no` (`order_no`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_payment_status` (`payment_status`),
  KEY `idx_created_at` (`created_at`),
  CONSTRAINT `fk_order_user` FOREIGN KEY (`user_id`) REFERENCES `users` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='订单表';
```

### 2.6 诊断报告表 (diagnose_reports)

```sql
CREATE TABLE `diagnose_reports` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '报告ID',
  `report_no` VARCHAR(50) NOT NULL COMMENT '报告编号',
  `user_id` BIGINT UNSIGNED NOT NULL COMMENT '用户ID',
  `company_name` VARCHAR(200) NOT NULL COMMENT '公司名称',
  `company_domain` VARCHAR(255) NOT NULL COMMENT '官网域名',
  `industry` VARCHAR(50) DEFAULT NULL COMMENT '行业',
  `overall_score` DECIMAL(5,2) DEFAULT 0.00 COMMENT '综合健康分 0-100',
  `score_trend` DECIMAL(5,2) DEFAULT 0.00 COMMENT '分数变化趋势',
  `health_dimensions` JSON NOT NULL COMMENT '各维度评分 {D1-D7}',
  `industry_benchmark` JSON DEFAULT NULL COMMENT '行业基准数据',
  `competitor_analysis` JSON DEFAULT NULL COMMENT '竞品分析',
  `issue_list` JSON DEFAULT NULL COMMENT '问题清单',
  `recommendations` JSON DEFAULT NULL COMMENT '优化建议',
  `diagnose_config` JSON DEFAULT NULL COMMENT '诊断配置',
  `status` ENUM('pending', 'running', 'completed', 'failed') DEFAULT 'pending' COMMENT '状态',
  `progress` INT DEFAULT 0 COMMENT '进度 0-100',
  `error_message` TEXT DEFAULT NULL COMMENT '错误信息',
  `started_at` DATETIME DEFAULT NULL COMMENT '开始时间',
  `completed_at` DATETIME DEFAULT NULL COMMENT '完成时间',
  `expires_at` DATETIME DEFAULT NULL COMMENT '报告过期时间',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_report_no` (`report_no`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_company_domain` (`company_domain`),
  KEY `idx_status` (`status`),
  KEY `idx_overall_score` (`overall_score`),
  KEY `idx_created_at` (`created_at`),
  CONSTRAINT `fk_diagnose_user` FOREIGN KEY (`user_id`) REFERENCES `users` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='诊断报告表';
```

### 2.7 策略方案表 (strategy_plans)

```sql
CREATE TABLE `strategy_plans` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '方案ID',
  `plan_no` VARCHAR(50) NOT NULL COMMENT '方案编号',
  `user_id` BIGINT UNSIGNED NOT NULL COMMENT '用户ID',
  `report_id` BIGINT UNSIGNED DEFAULT NULL COMMENT '关联报告ID',
  `plan_type` ENUM('brand_qa', 'product_desc', 'industry_view', 'competitor_compare', 'news_dynamic') NOT NULL COMMENT '策略类型',
  `title` VARCHAR(200) NOT NULL COMMENT '方案标题',
  `summary` TEXT COMMENT '方案摘要',
  `target_engines` JSON NOT NULL COMMENT '目标AI引擎',
  `content_style` JSON DEFAULT NULL COMMENT '内容风格偏好',
  `generated_strategies` JSON DEFAULT NULL COMMENT '生成的策略内容',
  `status` ENUM('draft', 'generated', 'approved', 'applied', 'archived') DEFAULT 'draft' COMMENT '状态',
  `usage_count` INT DEFAULT 0 COMMENT '使用次数',
  `effect_score` DECIMAL(5,2) DEFAULT NULL COMMENT '效果评分',
  `approved_at` DATETIME DEFAULT NULL COMMENT '审批时间',
  `applied_at` DATETIME DEFAULT NULL COMMENT '应用时间',
  `expires_at` DATETIME DEFAULT NULL COMMENT '过期时间',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_plan_no` (`plan_no`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_report_id` (`report_id`),
  KEY `idx_plan_type` (`plan_type`),
  KEY `idx_status` (`status`),
  CONSTRAINT `fk_plan_user` FOREIGN KEY (`user_id`) REFERENCES `users` (`id`) ON DELETE CASCADE,
  CONSTRAINT `fk_plan_report` FOREIGN KEY (`report_id`) REFERENCES `diagnose_reports` (`id`) ON DELETE SET NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='策略方案表';
```

### 2.8 生成内容表 (generated_contents)

```sql
CREATE TABLE `generated_contents` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '内容ID',
  `content_no` VARCHAR(50) NOT NULL COMMENT '内容编号',
  `user_id` BIGINT UNSIGNED NOT NULL COMMENT '用户ID',
  `plan_id` BIGINT UNSIGNED DEFAULT NULL COMMENT '关联策略ID',
  `content_type` ENUM('seo_article', 'xiaohongshu', 'qa', 'json_ld', 'outer_link_text', 'other') NOT NULL COMMENT '内容类型',
  `title` VARCHAR(500) NOT NULL COMMENT '内容标题',
  `content_body` LONGTEXT NOT NULL COMMENT '内容正文',
  `meta_info` JSON DEFAULT NULL COMMENT '元信息',
  `target_platform` VARCHAR(50) DEFAULT NULL COMMENT '目标平台',
  `target_engines` JSON DEFAULT NULL COMMENT '目标AI引擎',
  `status` ENUM('draft', 'generated', 'approved', 'published', 'archived') DEFAULT 'draft' COMMENT '状态',
  `published_at` DATETIME DEFAULT NULL COMMENT '发布时间',
  `publish_url` VARCHAR(500) DEFAULT NULL COMMENT '发布链接',
  `usage_count` INT DEFAULT 0 COMMENT '使用次数',
  `effect_metrics` JSON DEFAULT NULL COMMENT '效果指标',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_content_no` (`content_no`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_plan_id` (`plan_id`),
  KEY `idx_content_type` (`content_type`),
  KEY `idx_status` (`status`),
  KEY `idx_created_at` (`created_at`),
  FULLTEXT KEY `ft_title_content` (`title`, `content_body`(500)),
  CONSTRAINT `fk_content_user` FOREIGN KEY (`user_id`) REFERENCES `users` (`id`) ON DELETE CASCADE,
  CONSTRAINT `fk_content_plan` FOREIGN KEY (`plan_id`) REFERENCES `strategy_plans` (`id`) ON DELETE SET NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='生成内容表';
```

### 2.9 竞品监控表 (competitor_monitors)

```sql
CREATE TABLE `competitor_monitors` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '监控ID',
  `user_id` BIGINT UNSIGNED NOT NULL COMMENT '用户ID',
  `competitor_name` VARCHAR(200) NOT NULL COMMENT '竞品名称',
  `competitor_domain` VARCHAR(255) DEFAULT NULL COMMENT '竞品官网',
  `industry` VARCHAR(50) DEFAULT NULL COMMENT '行业',
  `keywords` JSON NOT NULL COMMENT '监控关键词',
  `mention_stats` JSON DEFAULT NULL COMMENT '提及统计数据',
  `mention_history` JSON DEFAULT NULL COMMENT '提及历史',
  `alert_enabled` TINYINT(1) DEFAULT 1 COMMENT '开启告警',
  `alert_threshold` INT DEFAULT 10 COMMENT '告警阈值(%)',
  `last_check_at` DATETIME DEFAULT NULL COMMENT '最后检查时间',
  `status` ENUM('active', 'paused', 'archived') DEFAULT 'active' COMMENT '状态',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_status` (`status`),
  KEY `idx_last_check` (`last_check_at`),
  CONSTRAINT `fk_competitor_user` FOREIGN KEY (`user_id`) REFERENCES `users` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='竞品监控表';
```

### 2.10 内容偏好模板表 (content_templates)

```sql
CREATE TABLE `content_templates` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '模板ID',
  `template_no` VARCHAR(50) NOT NULL COMMENT '模板编号',
  `engine` VARCHAR(50) NOT NULL COMMENT 'AI引擎',
  `category` ENUM('brand_qa', 'product_desc', 'industry_view', 'competitor_compare', 'news_dynamic') NOT NULL COMMENT '模板类别',
  `name` VARCHAR(200) NOT NULL COMMENT '模板名称',
  `description` TEXT COMMENT '模板描述',
  `template_content` LONGTEXT NOT NULL COMMENT '模板内容',
  `variables` JSON DEFAULT NULL COMMENT '变量定义',
  `prompt_template` TEXT NOT NULL COMMENT 'AI提示词模板',
  `output_format` ENUM('markdown', 'json', 'html', 'text') DEFAULT 'markdown' COMMENT '输出格式',
  `style_features` JSON DEFAULT NULL COMMENT '风格特征',
  `usage_count` INT DEFAULT 0 COMMENT '使用次数',
  `rating` DECIMAL(3,2) DEFAULT NULL COMMENT '平均评分',
  `is_system` TINYINT(1) DEFAULT 0 COMMENT '是否系统模板',
  `is_active` TINYINT(1) DEFAULT 1 COMMENT '是否启用',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_template_no` (`template_no`),
  KEY `idx_engine` (`engine`),
  KEY `idx_category` (`category`),
  KEY `idx_is_active` (`is_active`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='内容偏好模板表';
```

---

## 3. 扩展表结构

### 3.1 语义实体库表 (semantic_entities)

```sql
CREATE TABLE `semantic_entities` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '实体ID',
  `entity_type` ENUM('brand', 'industry_term', 'user_scenario', 'competitor', 'emotion_word', 'cta_word') NOT NULL COMMENT '实体类型',
  `entity_name` VARCHAR(200) NOT NULL COMMENT '实体名称',
  `synonyms` JSON DEFAULT NULL COMMENT '同义词列表',
  `weight` INT DEFAULT 50 COMMENT '权重 1-100',
  `description` TEXT COMMENT '描述',
  `industry` VARCHAR(50) DEFAULT NULL COMMENT '适用行业',
  `is_system` TINYINT(1) DEFAULT 0 COMMENT '是否系统实体',
  `user_id` BIGINT UNSIGNED DEFAULT NULL COMMENT '用户自定义时关联',
  `status` ENUM('active', 'inactive') DEFAULT 'active' COMMENT '状态',
  `usage_count` INT DEFAULT 0 COMMENT '使用次数',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_entity_type` (`entity_type`),
  KEY `idx_industry` (`industry`),
  KEY `idx_is_system` (`is_system`),
  KEY `idx_user_id` (`user_id`),
  FULLTEXT KEY `ft_entity_name` (`entity_name`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='语义实体库表';
```

### 3.2 通知记录表 (notifications)

```sql
CREATE TABLE `notifications` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '通知ID',
  `user_id` BIGINT UNSIGNED NOT NULL COMMENT '用户ID',
  `type` ENUM('diagnose_complete', 'strategy_generated', 'content_approved', 'competitor_alert', 'subscription_expiry', 'system') NOT NULL COMMENT '通知类型',
  `title` VARCHAR(200) NOT NULL COMMENT '通知标题',
  `content` TEXT NOT NULL COMMENT '通知内容',
  `data` JSON DEFAULT NULL COMMENT '扩展数据',
  `is_read` TINYINT(1) DEFAULT 0 COMMENT '是否已读',
  `read_at` DATETIME DEFAULT NULL COMMENT '阅读时间',
  `channel` ENUM('in_app', 'email', 'sms', 'wechat') DEFAULT 'in_app' COMMENT '通知渠道',
  `sent_at` DATETIME DEFAULT NULL COMMENT '发送时间',
  `expires_at` DATETIME DEFAULT NULL COMMENT '过期时间',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_type` (`type`),
  KEY `idx_is_read` (`is_read`),
  KEY `idx_created_at` (`created_at`),
  CONSTRAINT `fk_notification_user` FOREIGN KEY (`user_id`) REFERENCES `users` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='通知记录表';
```

### 3.3 操作日志表 (operation_logs)

```sql
CREATE TABLE `operation_logs` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '日志ID',
  `user_id` BIGINT UNSIGNED DEFAULT NULL COMMENT '用户ID',
  `username` VARCHAR(100) DEFAULT NULL COMMENT '用户名',
  `module` VARCHAR(50) NOT NULL COMMENT '模块',
  `action` VARCHAR(100) NOT NULL COMMENT '操作',
  `resource_type` VARCHAR(50) DEFAULT NULL COMMENT '资源类型',
  `resource_id` VARCHAR(100) DEFAULT NULL COMMENT '资源ID',
  `method` VARCHAR(20) DEFAULT NULL COMMENT '请求方法',
  `path` VARCHAR(500) DEFAULT NULL COMMENT '请求路径',
  `request_params` JSON DEFAULT NULL COMMENT '请求参数',
  `response_status` INT DEFAULT NULL COMMENT '响应状态',
  `ip_address` VARCHAR(50) DEFAULT NULL COMMENT 'IP地址',
  `user_agent` VARCHAR(500) DEFAULT NULL COMMENT '浏览器UA',
  `duration` INT DEFAULT NULL COMMENT '耗时(ms)',
  `error_message` TEXT DEFAULT NULL COMMENT '错误信息',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_module_action` (`module`, `action`),
  KEY `idx_resource` (`resource_type`, `resource_id`),
  KEY `idx_created_at` (`created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='操作日志表';
```

### 3.4 AI引擎配置表 (ai_engine_configs)

```sql
CREATE TABLE `ai_engine_configs` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '配置ID',
  `engine_code` VARCHAR(50) NOT NULL COMMENT '引擎代码',
  `engine_name` VARCHAR(100) NOT NULL COMMENT '引擎名称',
  `api_endpoint` VARCHAR(500) NOT NULL COMMENT 'API地址',
  `api_key` VARCHAR(500) DEFAULT NULL COMMENT 'API密钥(加密存储)',
  `model` VARCHAR(100) DEFAULT NULL COMMENT '模型名称',
  `max_tokens` INT DEFAULT 4096 COMMENT '最大Token数',
  `temperature` DECIMAL(3,2) DEFAULT 0.7 COMMENT '温度参数',
  `rate_limit_rpm` INT DEFAULT 60 COMMENT '每分钟限制',
  `rate_limit_tpm` INT DEFAULT 60000 COMMENT '每分钟Token限制',
  `is_enabled` TINYINT(1) DEFAULT 1 COMMENT '是否启用',
  `status` ENUM('active', 'inactive', 'error') DEFAULT 'active' COMMENT '状态',
  `last_test_at` DATETIME DEFAULT NULL COMMENT '最后测试时间',
  `last_error` TEXT DEFAULT NULL COMMENT '最后错误',
  `cost_per_1k_tokens` DECIMAL(8,6) DEFAULT NULL COMMENT '每1K Token成本',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_engine_code` (`engine_code`),
  KEY `idx_is_enabled` (`is_enabled`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='AI引擎配置表';
```

### 3.5 优惠券表 (coupons)

```sql
CREATE TABLE `coupons` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '优惠券ID',
  `code` VARCHAR(50) NOT NULL COMMENT '优惠券代码',
  `name` VARCHAR(200) NOT NULL COMMENT '优惠券名称',
  `type` ENUM('fixed', 'percentage', 'trial') NOT NULL COMMENT '类型',
  `value` DECIMAL(10,2) DEFAULT NULL COMMENT '优惠值',
  `percentage` INT DEFAULT NULL COMMENT '折扣百分比',
  `min_order_amount` DECIMAL(10,2) DEFAULT 0.00 COMMENT '最低订单金额',
  `max_discount_amount` DECIMAL(10,2) DEFAULT NULL COMMENT '最高优惠金额',
  `package_id` BIGINT UNSIGNED DEFAULT NULL COMMENT '适用套餐',
  `tier` ENUM('basic', 'professional', 'enterprise') DEFAULT NULL COMMENT '适用等级',
  `valid_from` DATETIME NOT NULL COMMENT '有效期开始',
  `valid_until` DATETIME NOT NULL COMMENT '有效期结束',
  `total_quantity` INT DEFAULT NULL COMMENT '总数量',
  `used_quantity` INT DEFAULT 0 COMMENT '已使用数量',
  `per_user_limit` INT DEFAULT 1 COMMENT '每人限用次数',
  `is_active` TINYINT(1) DEFAULT 1 COMMENT '是否启用',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_code` (`code`),
  KEY `idx_is_active` (`is_active`),
  KEY `idx_valid_period` (`valid_from`, `valid_until`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='优惠券表';
```

---

## 4. 初始化数据

```sql
-- 插入套餐数据
INSERT INTO `packages` (`code`, `name`, `tier`, `price_monthly`, `price_yearly`, `diagnose_engines`, `diagnose_questions`, `content_limit_monthly`, `competitor_limit`) VALUES
('basic', '基础版', 'basic', 999.00, 9999.00, 1, 10, 10, 1),
('professional', '专业版', 'professional', 2999.00, 29999.00, 6, 100, 100, 3),
('enterprise', '企业版', 'enterprise', 9999.00, 99999.00, 10, -1, -1, -1);

-- 插入AI引擎配置
INSERT INTO `ai_engine_configs` (`engine_code`, `engine_name`, `api_endpoint`, `model`, `rate_limit_rpm`) VALUES
('deepseek', 'DeepSeek', 'https://api.deepseek.com/v1', 'deepseek-chat', 60),
('doubao', '豆包', 'https://ark.cn-beijing.volces.com/api/v3', 'doubao-pro', 120),
('ernie', '文心一言', 'https://aip.baidubce.com/rpc/2.0/ai_custom/v1', 'ernie-4.0-8k-latest', 30),
('kimi', 'Kimi', 'https://api.moonshot.cn/v1', 'moonshot-v1-8k', 60),
('tongyi', '通义千问', 'https://dashscope.aliyuncs.com/api/v1', 'qwen-turbo', 120),
('zhipu', '智谱清言', 'https://open.bigmodel.cn/api/paas/v4', 'glm-4', 60);
```
